name: 'Publish Immutable Action'

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout code
      - uses: actions/checkout@v5

      # 2️⃣ Generate GitHub App token using Wasabi Sync Bot
      - name: Create SYNC_BOT_TOKEN
        id: sync-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.SYNC_BOT_APP_ID }}
          private-key: ${{ secrets.SYNC_BOT_PRIVATE_KEY }}
          permission-contents: write
          permission-packages: write

      # 3️⃣ Publish Immutable Action using generated token
      - name: Publish Immutable Action
        uses: actions/publish-immutable-action@v0.0.4
        with:
          token: ${{ steps.sync-token.outputs.token }}
